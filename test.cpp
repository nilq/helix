#include "test.hpp"

int bird::hello(int name,int age) {
	printf("hello, %i, age %i",name,age);
return 1;
;
}
namespace bar {
	auto foo(int a,int b) {
	int a1 = 100;;
if((a + (b == 0))) {return a1;
};
return (a + b);
;
}
}int main() {
	bird bob;;
bob.hello(1,2);
return bar::foo(1,-1);
;
}