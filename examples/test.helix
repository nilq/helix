# test.helix
import "iostream" library
import "string" library

def test(int a)
	if a == 6
		return 2 + a
	else
		return 2 - a

def main()
	printf("test hello")

	return test(5) # success

module something
	foo = "bar"

	module inside
		foo = "hey"